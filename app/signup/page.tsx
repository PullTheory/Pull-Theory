"use client";
import { FormEvent, useEffect, useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { getSupabaseClient } from "../lib/supabase";
import { recordTrafficEvent } from "../components/TrafficTracker";

export default function SignupPage(){
const router=useRouter();const[email,setEmail]=useState("");const[username,setUsername]=useState("");const[referralCode,setReferralCode]=useState("");const[password,setPassword]=useState("");const[showPassword,setShowPassword]=useState(false);const[showReferral,setShowReferral]=useState(false);const[message,setMessage]=useState("");const[confirmationEmail,setConfirmationEmail]=useState<string|null>(null);const[loading,setLoading]=useState(false);const[termsAccepted,setTermsAccepted]=useState(false);const[checkingAccount,setCheckingAccount]=useState(true);const[returnTo,setReturnTo]=useState("/binders");const furthestStep=useRef("opened");const completed=useRef(false);
function progress(step:string){furthestStep.current=step;void recordTrafficEvent(`signup_reached_${step}`)}
useEffect(()=>{const p=new URLSearchParams(window.location.search);const requested=p.get("returnTo");const ref=p.get("ref")||p.get("referral");if(ref){setReferralCode(ref.toUpperCase());setShowReferral(true)}const safe=requested?.startsWith("/")&&!requested.startsWith("//")?requested:"/binders";setReturnTo(safe);async function check(){const s=getSupabaseClient();const{data}=s?await s.auth.getSession():{data:{session:null}};if(data.session?.user){void recordTrafficEvent("signup_existing_session");router.replace(safe);return}setCheckingAccount(false);void recordTrafficEvent("signup_opened")}void check();return()=>{if(!completed.current)void recordTrafficEvent(`signup_abandoned_after_${furthestStep.current}`)}},[router]);
async function handleSignup(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMessage("");setConfirmationEmail(null);if(!termsAccepted){void recordTrafficEvent("signup_error_terms");setMessage("Please accept the Pull Theory Terms and PullShield Rules to continue.");setLoading(false);return}const u=username.trim();if(!/^[a-zA-Z0-9_-]{3,24}$/.test(u)){void recordTrafficEvent("signup_error_username");setMessage("Choose a username with 3–24 letters, numbers, hyphens, or underscores.");setLoading(false);return}void recordTrafficEvent("signup_submitted");try{const response=await fetch("/api/auth/signup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password,confirmPassword:password,username:u,plan:"collector",referralCode:referralCode.trim(),termsAccepted})});const result=await response.json().catch(()=>({}));if(!response.ok){void recordTrafficEvent(`signup_error_${response.status}`);setMessage(result?.error||"We couldn't create your account right now. Please try again.")}else{completed.current=true;setConfirmationEmail(email.trim());void recordTrafficEvent(result?.existing?"signup_existing_account":"confirmation_sent")}}catch{void recordTrafficEvent("signup_error_network");setMessage("We couldn't reach the signup service. Please try again.")}setLoading(false)}
if(checkingAccount)return <main className="min-h-screen bg-black"/>;const loginHref=`/login?returnTo=${encodeURIComponent(returnTo)}`;return <main className="min-h-screen bg-black px-5 py-8 text-white"><div className="mx-auto max-w-md rounded-3xl border border-violet-500/30 bg-white/5 p-7 shadow-2xl"><a href="/" className="text-sm font-bold uppercase tracking-[0.2em] text-violet-300">Pull Theory HQ</a><p className="mt-7 text-xs font-bold uppercase tracking-[0.2em] text-amber-300">Free collector account</p><h1 className="mt-2 text-3xl font-black">Build your Pokémon collection.</h1><p className="mt-2 text-zinc-400">Create your free account, start your digital binder, and discover safer ways to buy, sell, and trade. No credit card required.</p><div className="mt-5 grid grid-cols-3 gap-2 text-center text-xs"><div className="rounded-xl border border-white/10 bg-black/30 p-3"><b className="block text-white">Free</b><span className="text-zinc-500">to join</span></div><div className="rounded-xl border border-white/10 bg-black/30 p-3"><b className="block text-white">Binder</b><span className="text-zinc-500">included</span></div><div className="rounded-xl border border-white/10 bg-black/30 p-3"><b className="block text-white">PullShield</b><span className="text-zinc-500">available</span></div></div><form onSubmit={handleSignup} className="mt-7 space-y-4"><div><label className="mb-2 block text-sm text-zinc-300">Public username</label><input required minLength={3} maxLength={24} autoComplete="username" value={username} onFocus={()=>progress("username")} onChange={e=>setUsername(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 outline-none focus:border-violet-400" placeholder="CardCollector"/></div><div><label className="mb-2 block text-sm text-zinc-300">Email</label><input type="email" required autoComplete="email" value={email} onFocus={()=>progress("email")} onChange={e=>setEmail(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 outline-none focus:border-violet-400" placeholder="you@example.com"/></div><div><label className="mb-2 block text-sm text-zinc-300">Password</label><div className="relative"><input type={showPassword?"text":"password"} required minLength={6} autoComplete="new-password" value={password} onFocus={()=>progress("password")} onChange={e=>setPassword(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 pr-16 outline-none focus:border-violet-400" placeholder="6+ characters"/><button type="button" onClick={()=>setShowPassword(v=>!v)} className="absolute inset-y-0 right-0 px-4 text-sm font-semibold text-violet-200">{showPassword?"Hide":"Show"}</button></div></div>{!showReferral?<button type="button" onClick={()=>setShowReferral(true)} className="text-sm text-zinc-500 underline">Have an ambassador/referral code?</button>:<div><label className="mb-2 block text-sm text-zinc-300">Referral code <span className="text-zinc-500">(optional)</span></label><input value={referralCode} onChange={e=>setReferralCode(e.target.value.toUpperCase())} maxLength={24} className="w-full rounded-xl border border-amber-300/20 bg-black/40 px-4 py-3 uppercase outline-none focus:border-amber-300" placeholder="AMBASSADOR CODE"/></div>}<label className="flex cursor-pointer items-start gap-3 text-sm leading-6 text-zinc-400"><input type="checkbox" required checked={termsAccepted} onChange={e=>{setTermsAccepted(e.target.checked);if(e.target.checked)progress("terms")}} className="mt-1 h-4 w-4 accent-violet-500"/><span>I agree to the <a href="/terms" target="_blank" className="text-amber-200 underline">Terms and PullShield Rules</a>.</span></label>{message&&<p className="rounded-xl bg-violet-500/10 p-3 text-sm text-violet-200">{message}</p>}<button disabled={loading} className="w-full rounded-xl bg-violet-600 px-4 py-4 text-lg font-black disabled:opacity-60">{loading?"Creating account...":"Start my free binder"}</button></form><p className="mt-4 text-center text-xs text-zinc-500">Membership upgrades are optional. <a href="/pricing" className="text-zinc-300 underline">See membership perks</a></p><p className="mt-5 text-center text-sm text-zinc-400">Already have an account? <a href={loginHref} className="font-medium text-amber-300">Log in</a></p></div>{confirmationEmail&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 px-6"><div className="w-full max-w-md rounded-3xl border border-violet-400/40 bg-zinc-950 p-7 text-center"><p className="text-xs font-bold uppercase tracking-[0.2em] text-amber-300">One quick step</p><h2 className="mt-2 text-3xl font-black">Check your email</h2><p className="mt-4 text-sm text-zinc-300">We sent a confirmation link to <b>{confirmationEmail}</b>. Confirm it, then start building your binder.</p><a href={loginHref} className="mt-6 block rounded-xl bg-violet-600 px-4 py-3 font-semibold">Continue to my binder</a></div></div>}</main>}
